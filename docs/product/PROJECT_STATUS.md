WorkShift checkpoint saved on 2026-03-29.

Current status:
- Landing page and signup flow were actively iterated.
- Worker/employer/admin prototype flows exist in the project.
- Voice and assisted signup are UI-guided prototype flows, not real backend integrations.
- `index.template.html` is the editable source template.
- `index.html` is the rebuilt output used for local preview.

Recommended starting point if revisiting later:
- Review `index.template.html`
- Rebuild with `scripts/build-bundle.ps1`

Project state:
- Paused
- Saved as baseline before switching to a new project
